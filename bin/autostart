#!/usr/bin/env bash

run() {
    if ! pgrep -f "$1"; then
        "$@" &
    fi
}

# Applications
run "picom"
run "lxsession"
run "flameshot"
run "copyq"

# Screen config
run "$HOME/.config/screenlayout/triple-tv"
run "$HOME/.local/bin/disable-copy"
