#
# Launch awesome on /dev/tty1 login
#

# {% if !data.wayland %}
#<yolk> if [[ $(tty) = "/dev/tty1" ]]; then
    #<yolk> # iBus
    #<yolk> pgrep awesome || startx "$XDG_CONFIG_HOME"/X11/xinitrc
#<yolk> fi
# {% end %}

# vim: ft=sh
